import React, { FC } from 'react';
import { Button } from 'antd';
import './App.css';
import './App.less';

const App: FC = () => (
  <div className="App centerContainer">
    <Button type="primary">Welcome to React-TS-Antd</Button>
  </div>
);

export default App;